<%= form_for @errand do |f| %>
  <%= render partial: "shared/errors", locals: { object: @errand } %>

  <p>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.submit  %>
  </p>
<% end %>
